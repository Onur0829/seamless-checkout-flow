import onlaLogo from '@/assets/onla-logo.jpg';

const OnlaLogo = () => {
  return (
    <img 
      src={onlaLogo} 
      alt="Onla" 
      className="h-12 w-auto"
    />
  );
};

export default OnlaLogo;
